# Auto-generated by clone-site
stages:
  - deploy

pages:
  stage: deploy
  script:
    - echo "Deploying to GitLab Pages..."
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment:
    name: production
    url: https://$CI_PAGES_NAMESPACE.gitlab.io/$CI_PROJECT_NAME
